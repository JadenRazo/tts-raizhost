apiVersion: v1
kind: Service
metadata:
  name: tts-kokoro
  namespace: tts
  labels:
    app.kubernetes.io/name: tts-kokoro
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tts-kokoro
  ports:
    - name: http
      port: 8000
      targetPort: http
