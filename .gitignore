# Dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Build artifacts
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# Env / secrets
.env
.env*.local
!.env.example

# OS / editor
.DS_Store
*.pem
.vscode/
.idea/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Drizzle (generated migrations are checked in; only ignore working files)
drizzle/meta/_journal.json.lock

# Kokoro models — too large to commit; downloaded by scripts/bootstrap-models.sh
services/kokoro/models/

# Local data (the production paths are /var/lib/tts-raizhost — never colocated with the repo)
data/

# Python bytecode (services/kokoro)
__pycache__/
*.pyc
